<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: Apache-2.0 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Permissions consumed by the data-layer providers/repositories. These are
      declared here so the module's content-provider access is self-documenting;
      manifest-merging folds them into :app, which performs the runtime requests
      with rationale UI (CONVENTIONS.md §8, BUILD_SPEC §20).
    -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <!-- Voicemail Contract / Status (requires default-dialer or these system permissions) -->
    <uses-permission android:name="com.android.voicemail.permission.READ_VOICEMAIL" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="com.android.voicemail.permission.WRITE_VOICEMAIL" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

</manifest>
