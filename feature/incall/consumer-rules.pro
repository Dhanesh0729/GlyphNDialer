# Consumer ProGuard/R8 rules for :feature:incall
#
# This module exposes only Compose UI + a Hilt @HiltViewModel and pure Kotlin
# state/event types. Hilt, Compose, kotlinx-coroutines and AndroidX Navigation
# ship their own consumer rules, so nothing extra is required here.
#
# (Intentionally empty beyond this header — see CONVENTIONS.md §4.)
